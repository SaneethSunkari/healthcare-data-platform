# Analytics

Use this folder for dbt models, tests, and documentation built on top of the ingested healthcare warehouse tables.

Run the analytics project with:

```bash
../.venv/bin/dbt run --project-dir analytics --profiles-dir analytics
```
